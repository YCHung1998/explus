# 效能優化指南

## 已實作的優化

### 1. 選擇性更新機制
- **圖表更新頻率**: 預設每 3 幀更新一次（可調整）
- **熱力圖更新頻率**: 預設每 5 幀更新一次（可調整）
- **原因**: Altair 熱力圖生成非常耗時，減少更新頻率可大幅提升效能

### 2. 效能監控工具
- 新增 `PerformanceMonitor` 類別
- 可追蹤每個操作的執行時間
- 幫助識別效能瓶頸

### 3. 減少不必要的複製
- 優化 `score_map` 的複製邏輯
- 只在必要時進行資料複製

## 主要效能瓶頸分析

### 最耗時的操作（依順序）

1. **Altair 熱力圖生成** ⚠️ 最慢
   - 每次生成需要 50-200ms
   - 解決方案: 降低更新頻率（預設每 5 幀）

2. **Streamlit 圖表更新**
   - 每次更新需要 10-50ms
   - 解決方案: 降低更新頻率（預設每 3 幀）

3. **ONNX 模型推理**（如果使用 YOLO 模式）
   - 每次推理需要 20-100ms
   - 解決方案: 使用較小的模型或降低解析度

4. **影像處理操作**
   - Resize、ColorMap 等操作
   - 解決方案: 減少處理解析度

## 效能優化建議

### 立即可以做的調整

1. **降低熱力圖更新頻率**
   ```
   Update Heatmaps Every N frames: 10-20
   ```
   這會帶來最明顯的效能提升

2. **降低圖表更新頻率**
   ```
   Update Charts Every N frames: 5-10
   ```

3. **關閉不需要的視覺化**
   - 如果不需要熱力圖，可以註解掉相關程式碼
   - 只保留必要的狀態顯示

### 進階優化

1. **使用更高效的視覺化方法**
   - 考慮使用 `plotly` 替代 `altair`（更快）
   - 或使用 OpenCV 直接繪製熱力圖

2. **減少處理解析度**
   - 在 `PreprocessingConfig` 中降低 `process_wh`
   - 例如: (320, 180) -> (240, 135)

3. **使用多執行緒**
   - 將視覺化更新放在獨立執行緒
   - 但需要注意 Streamlit 的執行緒安全

4. **快取計算結果**
   - 對於不變的資料進行快取
   - 使用 `@st.cache_data` 裝飾器

## 效能監控使用方式

1. 在設定中啟用 "Enable Performance Monitor"
2. 執行應用程式
3. 觀察效能指標（每 30 幀顯示一次）
4. 識別最慢的操作
5. 針對性地進行優化

## 預期效能提升

- **降低熱力圖更新頻率到 10**: 提升 30-50% FPS
- **降低圖表更新頻率到 5**: 提升 10-20% FPS
- **關閉熱力圖**: 提升 50-70% FPS
- **降低處理解析度**: 提升 20-40% FPS

## 建議設定（平衡效能與視覺化）

- **Update Charts Every**: 3-5 frames
- **Update Heatmaps Every**: 10-15 frames
- **History Size**: 200-300（不要太大）
- **Process Resolution**: (320, 180) 或更小

## 故障排除

### 如果還是很慢

1. **檢查是否啟用了效能監控**
   - 監控本身會有一些開銷
   - 在生產環境可以關閉

2. **檢查硬體**
   - CPU 效能
   - 記憶體使用情況
   - 是否有其他程式佔用資源

3. **檢查 Streamlit 設定**
   - 確保使用最新版本
   - 檢查是否有其他 Streamlit 應用在運行

4. **考慮使用原生 OpenCV 視覺化**
   - 如果 Streamlit 太慢，可以回到 `main_v2.py` 的 OpenCV 模式
   - 使用 `debug_visual=True` 參數
